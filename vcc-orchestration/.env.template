# Chirality AI - Environment Template
# Copy this to individual service directories and customize as needed

# =============================================================================
# OpenAI Configuration (Required for all services)
# =============================================================================
OPENAI_API_KEY=sk-proj-your-api-key-here
OPENAI_MODEL=gpt-4.1-nano
DEFAULT_TEMPERATURE=0.6
MAX_OUTPUT_TOKENS=800

# =============================================================================
# Neo4j Configuration (Required for GraphQL service)
# =============================================================================
# Neo4j Aura (Cloud) - Recommended
NEO4J_URI=neo4j+s://your-instance.databases.neo4j.io
NEO4J_USER=neo4j
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=your-password-here
NEO4J_DATABASE=neo4j

# Neo4j Local (Alternative) - For local development only
# NEO4J_URI=bolt://localhost:7687
# NEO4J_USER=neo4j
# NEO4J_PASSWORD=changeme
# NEO4J_DATABASE=neo4j

# =============================================================================
# Service Ports (Default configuration)
# =============================================================================
# GraphQL Service
GRAPHQL_PORT=8080

# AI App (Document Generation)
AI_APP_PORT=3000

# Semantic Framework 
FRAMEWORK_PORT=3002

# Admin Service (Optional)
ADMIN_PORT=3001

# =============================================================================
# Service URLs (Auto-configured based on ports above)
# =============================================================================
NEXT_PUBLIC_GRAPHQL_URL=http://localhost:8080/graphql
NEXT_PUBLIC_ORCHESTRATOR_URL=http://localhost:3001
NEXT_PUBLIC_GRAPH_API=http://localhost:8080/graphql

# =============================================================================
# Feature Flags
# =============================================================================
NEXT_PUBLIC_USE_GRAPHQL=true
CF14_VERSION=2.1.1
CF14_ENABLE_ARRAY_PH=true
CF14_LEGACY_API_SUPPORT=true

# =============================================================================
# Development Settings
# =============================================================================
NODE_ENV=development
NEXT_TELEMETRY_DISABLED=1
DEBUG=false

# =============================================================================
# Instructions for Each Service
# =============================================================================

# For chirality-ai-app/.env.local:
# - Copy OPENAI_* variables
# - Copy NEXT_PUBLIC_* variables  
# - Add NEO4J_* variables (optional, for legacy features)

# For chirality-semantic-framework/.env.local:
# - Copy OPENAI_* variables
# - Copy NEO4J_* variables
# - Copy NEXT_PUBLIC_* variables

# For chirality-semantic-framework/graphql-service/.env:
# - Copy NEO4J_* variables
# - Set PORT=8080
# - Set NODE_ENV=development

# Example file structure:
# ai-env/
# ├── chirality-ai-app/.env.local
# ├── chirality-semantic-framework/.env.local
# ├── chirality-semantic-framework/graphql-service/.env
# └── chirality-ai/.env.template (this file)