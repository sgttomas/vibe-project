# Required for LLM calls in frontend API routes
OPENAI_API_KEY=

# Optional public flags
NEXT_PUBLIC_APP_NAME=Chirality Chat
NEXT_PUBLIC_DEBUG=0

# Optional model override (defaults to gpt-4.1-nano)
OPENAI_MODEL=gpt-4.1-nano

# Optional Neo4j for legacy GraphQL features (not required for core functionality)
NEO4J_URI=
NEO4J_USER=
NEO4J_PASSWORD=